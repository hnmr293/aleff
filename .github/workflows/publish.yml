name: Publish to PyPI

on:
  push:
    branches: [main]

jobs:
  check-version:
    name: Check version tag
    runs-on: ubuntu-latest
    outputs:
      should_publish: ${{ steps.check.outputs.should_publish }}
      tag: ${{ steps.version.outputs.tag }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Get version from pyproject.toml
        id: version
        run: |
          version=$(grep -Po '(?<=^version = ")[^"]+' pyproject.toml)
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=v$version" >> "$GITHUB_OUTPUT"

      - name: Check if tag exists
        id: check
        run: |
          if git rev-parse "refs/tags/${{ steps.version.outputs.tag }}" >/dev/null 2>&1; then
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
          else
            echo "should_publish=true" >> "$GITHUB_OUTPUT"
          fi

  build-sdist:
    name: Build sdist
    needs: check-version
    if: needs.check-version.outputs.should_publish == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: astral-sh/setup-uv@v8.0.0
        with:
          cache-suffix: sdist

      - name: Build sdist
        run: uv build --sdist

      - uses: actions/upload-artifact@v7
        with:
          name: dist-sdist
          path: dist/*.tar.gz
          if-no-files-found: error
          retention-days: 1

  build-wheels:
    name: "Build wheel (${{ matrix.identifier }})"
    needs: check-version
    if: needs.check-version.outputs.should_publish == 'true'
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - identifier: cp312-manylinux_x86_64
            runner: ubuntu-latest
          - identifier: cp313-manylinux_x86_64
            runner: ubuntu-latest
          - identifier: cp314-manylinux_x86_64
            runner: ubuntu-latest
          - identifier: cp314t-manylinux_x86_64
            runner: ubuntu-latest
          - identifier: cp312-win_amd64
            runner: windows-latest
          - identifier: cp313-win_amd64
            runner: windows-latest
          - identifier: cp314-win_amd64
            runner: windows-latest
          - identifier: cp314t-win_amd64
            runner: windows-latest
          - identifier: cp312-macosx_x86_64
            runner: macos-15-intel
          - identifier: cp313-macosx_x86_64
            runner: macos-15-intel
          - identifier: cp314-macosx_x86_64
            runner: macos-15-intel
          - identifier: cp314t-macosx_x86_64
            runner: macos-15-intel
          - identifier: cp312-macosx_arm64
            runner: macos-15
          - identifier: cp313-macosx_arm64
            runner: macos-15
          - identifier: cp314-macosx_arm64
            runner: macos-15
          - identifier: cp314t-macosx_arm64
            runner: macos-15
    steps:
      - uses: actions/checkout@v6

      - uses: astral-sh/setup-uv@v8.0.0

      - name: Build wheels
        uses: pypa/cibuildwheel@v3.4.1
        env:
          CIBW_BUILD: ${{ matrix.identifier }}

      - uses: actions/upload-artifact@v7
        with:
          name: dist-wheel-${{ matrix.identifier }}
          path: ./wheelhouse/*.whl
          if-no-files-found: error
          retention-days: 1

  publish-testpypi:
    name: Publish to TestPyPI
    needs: [build-sdist, build-wheels]
    runs-on: ubuntu-latest
    environment: testpypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: dist-*
          path: dist/
          merge-multiple: true

      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true

  publish-pypi:
    name: Publish to PyPI
    needs: publish-testpypi
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: dist-*
          path: dist/
          merge-multiple: true

      - uses: pypa/gh-action-pypi-publish@release/v1

  release:
    name: Create GitHub Release
    needs: [check-version, publish-pypi]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6

      - name: Create GitHub Release
        run: gh release create "${{ needs.check-version.outputs.tag }}" --target main --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
